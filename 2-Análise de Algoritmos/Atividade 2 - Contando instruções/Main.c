
int i,j,k,s;//Declaração(Declarar i,j,k,s como variavel do tipo inteiro).
for(i=0; i<n-1; i++)//Atribuição(i =inicia a 0); Comparação(i é menor que n-1, se sim continua, se não finaliza o for); Incrementa(i recebe soma + 1).
{
    for(j=i+1; j<n; j++)//Atribuição(j =inicia recebendo valor da variavel i+1); Comparação(j é menor que n, se sim continua, se não finaliza o for); Incrementa(j recebe soma + 1).
    {
        for(k=1; k<j; k++)//Atribuição(k =inicia a 1); Comparação(k é menor que n, se sim continua, se não finaliza o for); Incrementa(k recebe soma + 1).
        {
            s=1;//Atribuição(variavel s vale 1);
        }
    }
}

f(n)=3n+2
